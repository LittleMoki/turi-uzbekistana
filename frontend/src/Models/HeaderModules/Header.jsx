import { BottomMenu } from "./BottomMenu";

export const Header = () => {
    return (
        <header className="w-full sticky top-0 left-0 z-20">
            <BottomMenu />  
        </header>
    );
};
